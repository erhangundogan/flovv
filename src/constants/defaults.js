const defaults = {
  rect: {
    standard: {
      pointerEvents: 'all',
      width: 120,
      height: 80,
      item: 'rect'
    }
  },
  circle: {
    standard: {
      pointerEvents: 'all',
      r: 50,
      item: 'circle'
    }
  },
  grid: {
    size: 10
  }
};

export default defaults;
